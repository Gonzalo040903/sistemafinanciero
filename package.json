{
  "dependencies": {
    "express": "^4.21.1",
    "mongoose": "^8.7.3",
    "nodemon": "^3.1.7"
  },
  "scripts": {
  "install": "cd front && npm install",
  "build": "cd front && npm install && npm run build"
}
}
