// Temporal, después cambiamos por integración real

export async function enviarMensajeWhatsApp(numero, mensaje) {
    console.log(`Simulando envío de mensaje a ${numero}: ${mensaje}`);
    // Aquí luego pondremos la llamada a Twilio o Venom-bot
}
